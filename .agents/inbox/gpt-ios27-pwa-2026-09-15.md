# GPT → active GPT session: iOS 27 / Safari 27 FreightLogic tranche

Date: 2026-09-15
Operator instruction: start implementing every iOS 27 feature FreightLogic can safely use now.

## Coordination constraint

Current `app-js.lock` is held by GPT token `1ce16d0f-d134-4f59-9fef-8f78f250ad3a` through 2026-09-15T13:05:00Z and covers `app.js`, `index.html`, `service-worker.js`, `sw-bridge.js`, `modern-shell.js`, `manifest.json`. Do not create a competing SHARED lock. Fold this tranche into that session only if it can do so without destabilizing its bounded full-repair task; otherwise leave this request pending for the next lock.

Current main observed before handoff: `4f2daf22819feb8d7aeba40324e53ce971f22418` (`v24.0.12`).

## Verified Safari 27 facts

Official WebKit WWDC26 material says Safari 27 adds:
- customizable native `<select>` via `appearance: base-select`, with `::picker-icon`, `::checkmark`, and `<selectedcontent>`; progressive enhancement is explicitly recommended;
- scroll anchoring (automatic browser behavior; no code required unless our CSS opts out);
- Service Worker Static Routing API (`InstallEvent.addRoutes`) to bypass the worker for selected requests;
- ReadableStream async iteration, `ReadableStream.from()`, and transferable streams;
- major Service Worker/touch/ResizeObserver/IntersectionObserver correctness fixes;
- Safari MCP server for developer/agent browser inspection on macOS Safari 27 (workflow/tooling, not an iPhone runtime API).

Primary references:
- https://webkit.org/blog/17967/news-from-wwdc26-webkit-in-safari-27-beta/
- https://webkit.org/blog/18117/the-golden-rule-of-customizable-select/
- https://webkit.org/blog/18136/introducing-the-safari-mcp-server-for-web-developers/

## Implement now — safe PWA work

### 1. Safari 27 customizable select — YES
`styles.css` is GPT-owned and does not require the SHARED lock. Add progressive enhancement only:
- keep existing plain-text `<option>` labels; never replace text with icon-only content;
- wrap all new styling in `@supports (appearance: base-select)`;
- set `appearance: base-select` for selects in supporting browsers;
- use Safari 27 picker/icon pseudo-elements only as enhancement;
- preserve >=16px mobile form size, 44px touch-target expectations, dark/light themes, keyboard navigation and VoiceOver semantics;
- older Safari/Chrome/Firefox must retain the current fallback unchanged.

Do not redesign forms. This is native-control fidelity, not cosmetic churn.

### 2. Scroll anchoring — VERIFY, normally NO PATCH
Safari 27 now supplies scroll anchoring. Confirm FreightLogic does not globally set `overflow-anchor:none`. If not, record that FreightLogic automatically benefits. Do not add CSS merely to claim the feature. For dynamic Load Inbox / Trips / Expenses lists, add explicit `overflow-anchor` rules only if a real regression test demonstrates a need.

### 3. Service Worker Static Routing — IMPLEMENT ONLY IF SEMANTICALLY SAFE
Do **not** route app-shell/runtime assets straight to network merely because Safari 27 supports `addRoutes`; FreightLogic is offline-first and its current worker is hardened/production-certified.

Inventory the current worker first. Static routes are justified only for a stable same-origin request class whose existing semantics are already network-only and that is not part of offline, share-target, cache-generation, update or decision-critical behavior.

If such a path exists:
- feature-detect `event.addRoutes` in the `install` handler;
- declare the minimal route with `source: 'network'` (or another source only if Safari 27 support is verified from the current spec/implementation);
- failure/rejection to register a route must not abort worker install;
- older browsers must execute the current fetch handler unchanged;
- preserve share-target POST handling;
- preserve known-asset query-insensitive offline self-heal and 504 text/plain behavior;
- preserve one-generation cache cleanup and release marker discipline.

If no safe same-origin network-only path exists today, **do not add a decorative/no-op router**. Record `NOT YET APPLICABLE` rather than weakening offline semantics.

### 4. ReadableStream improvements — USE ONLY ON A PROVEN HOT PATH
Inspect large import/export, OCR/document, cloud backup/restore paths. Do not rewrite stable code simply to use a new API. If a large payload path can materially reduce memory/latency and can be feature-detected with unchanged fallback, implement it with regression coverage. Otherwise mark available-for-future without code churn.

### 5. Safari 27 MCP — QA workflow, not app runtime
Do not add browser-agent code to the shipped PWA. Add/prepare an operator/dev QA step only when the appropriate documentation/tooling lane is available: Safari 27 on macOS, Web Developer features + remote automation/external agents, then use `/usr/bin/safaridriver --mcp` for real Safari DOM/network/console/screenshot inspection. This does not replace physical-iPhone A1–A10 certification.

## Native-only features — DO NOT fake into the PWA
Do not claim or emulate App Intents/Siri onscreen awareness, Foundation Models, Core AI, WidgetKit, Live Activities, Spotlight App Entities, or native background inference from the current web app. Those require a future native iOS capability layer / wrapper. Keep the current architecture; no Swift rewrite.

## Tests / release gates

Any SHARED/app.js/service-worker change must obey existing lock + full-suite rules. Preserve exact release-generation discipline. Add negative controls for any new Safari 27-specific behavior. Before merge, run current full suite, lane guard, exact-head CI, live parity, and production SW gate as applicable. Do not declare physical iPhone A1–A10 or M6 closed.
