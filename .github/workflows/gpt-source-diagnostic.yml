name: GPT Write Probe
on:
  push:
    branches: [agent/gpt/final-source-integrity]
    paths: ['.github/workflows/gpt-source-diagnostic.yml']
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo WRITE_PROBE_OK
